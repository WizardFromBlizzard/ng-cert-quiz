import { LetterSearchPipe } from './letter-search.pipe';

describe('LetterSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new LetterSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
